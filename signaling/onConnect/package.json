{
  "name": "@liaison/signaling-connect",
  "version": "1.0.0",
  "description": "Lambda to handle $connect websocket routeKey",
  "main": "index.js",
  "scripts": {
    "build": "yarn install",
    "clean": "rm -rf node_modules lambda.zip lambda",
    "postinstall": "ln -s ../../node_modules node_modules",
    "release": "yarn run build && yarn run zip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r lambda.zip * -x *signaling-connect*"
  },
  "keywords": [
    "liaison",
    "signaling",
    "$connect"
  ],
  "author": "Luis Valencia",
  "license": "ISC",
  "dependencies": {
    "@liaison/common-dynamo-data-repository": "1.0.0"
  }
}
